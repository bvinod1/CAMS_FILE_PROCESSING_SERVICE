package com.cams.fileprocessing.features.validation.models;

/**
 * Data types supported for column-level validation rules.
 */
public enum ColumnDataType {
    STRING,
    INTEGER,
    DECIMAL,
    DATE,
    UUID
}
